{
  "name": "devops-app",
  "version": "1.0.0",
  "description": "Node.js app for Devops Jenkins pipeline",
  "main": "app.js",
  "scripts": {
    "test": "jest --ci --reporters=default --reporters=jest-junit"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "jest": "^29.5.0",
    "jest-junit": "^13.0.0",
    "supertest": "^6.3.3"
  }
}
